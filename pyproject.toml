[project]
name = "deskos"
version = "0.1.0"
description = "Local-first personal productivity and engineering work management desktop app"
requires-python = ">=3.8"
dependencies = [
	"typing_extensions>=4.0; python_version < '3.10'",
]

[tool.unittest]
start-directory = "tests"
